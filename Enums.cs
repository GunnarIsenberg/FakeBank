using System;

namespace GunnarsBank
{
    public enum AccountType
    {
        Loan,
        Banking,
        Storage,
        FinancialServices
    }

    public enum TransactionType
    {
        Deposit,
        Withdraw,
    }
}